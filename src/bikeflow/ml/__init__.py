"""Data and model pipeline: preprocessing, temporal split, training, evaluation.

Owned by participant A. The API depends on this package only through
`bikeflow.model.adapter`, never by importing training code directly.
"""
